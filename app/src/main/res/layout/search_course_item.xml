<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/search_course_item_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1.0"
            android:textSize="24sp"
            android:textColor="?attr/examsNameTextColor"
            android:text="Course" />

        <ImageView
            android:id="@+id/search_course_btn_add"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:focusable="true"
            android:clickable="true"
            android:background="?selectableItemBackgroundBorderless"
            android:src="@drawable/ic_btn_add" />

    </LinearLayout>

    <TextView
        android:id="@+id/search_course_item_teacher"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        android:textColor="?attr/examsNameTextColor"
        android:text="Teacher" />

    <TextView
        android:id="@+id/search_course_item_schedule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        android:textColor="?attr/examsNameTextColor"
        android:text="Schedule" />

    <TextView
        android:id="@+id/search_course_item_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        android:textColor="?attr/examsNameTextColor"
        android:text="Detail" />

</LinearLayout>